//! GBA Core - Core execute engine
//!
//! This crate provides the core execution engine for the GBA CLI,
//! built on top of the Claude Agent SDK.

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
